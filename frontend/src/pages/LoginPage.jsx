import "../App.css";

// 임시 화면입니다. 3단계에서 실제 로그인 폼(LoginForm)이 여기 들어옵니다.
function LoginPage() {
  return (
    <div className="app">
      <h1>로그인</h1>
      <section className="panel">
        <p>로그인 화면 (추후 구현)</p>
      </section>
    </div>
  );
}

export default LoginPage;
